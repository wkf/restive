#!/usr/bin/env node

require('coffee-script')

var path   = require('path');
var fs     = require('fs');
var client = path.join(path.dirname(fs.realpathSync(__filename)), '../client');

new (require(client)());
